-- Start of the script on GitHub (testy)

-- The original script content as a string
local original_script = [=[
    -- Your actual script content goes here
    print("Hello, world! This is the original script.")
]=]

-- Unique identifier for validation (can be a hash or any unique string)
local script_signature = "unique_signature_123456789"

-- Function to validate that no extra code has been added
local function validate_script()
    -- Check if the environment is clean and no extra code is running
    local info = debug.getinfo(3, "S")
    if info and (info.what ~= "main" or info.short_src ~= "[C]") then
        error("Modified loadstring detected! Execution stopped.")
    end

    -- Validate that the script contains the expected signature
    if not original_script:find(script_signature) then
        error("Script validation failed: content altered!")
    end
end

-- Execute the validation
validate_script()

-- If validation passes, run the original script
loadstring(original_script)()

-- End of the script on GitHub (testy)
